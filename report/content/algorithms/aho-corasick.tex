\subsection[The Aho-Corasick algorithm]{The Aho-Corasick algorithm\cite{cpalgorithms_ahocorasick}}
    \subsubsection{Introduction \& Application}
        \begin{itemize}
            \item \textbf{Core idea:} Aho-Corasick is a DFA (Deterministic Finite Automaton) which uses a Trie with KMP suffix links added in.
            It builds a Trie tree for all given patterns and links nodes to the longest proper suffix that is also a prefix in the Trie. 
            If the algorithm hits a mismatch while processing, it just jumps to the next possible suffix by following the link instead of starting over. 
            This keeps the search moving forward in one pass and if it reaches a output-node, it identifies a pattern match within the text.
            \item \textbf{Real-world applications:}
            \begin{itemize}
                \item Bioinformatics: Searching for specific DNA.
                \item Checking for Plagiarism: Comparing documents to find and compare a script or a piece of code with the exact match in a large database.
                \item Text Mining: Extracting specific entities, such as names of cities or organizations, from massive datasets to analyze trends and information.
                \item Intrusion Detection: Comparing incoming network traffic against a database of known attack signatures to prevent security threats.
            \end{itemize}
        \end{itemize}

    \subsubsection{Pseudocode}
        \textbf{Notation:}
        \begin{itemize}
            \item \textbf{Trie Node}: A structure containing a character $pC$, a list of pattern indices $pos$, child pointers $nxt$, transition pointers $go$, a suffix link $link$, and a parent pointer $par$.
            \item \textbf{Text $T$ (R rows, C columns)}: The 2D grid of characters.
            \item \textbf{Pattern $P$ (K keywords)}: The list of patterns to search for.
            \item \textbf{List\_string}: A 1D array of size $M = R + C$, containing all rows followed by all columns extracted from $T$.
        \end{itemize}

        \textsc{Add-String}($S, idx, root$)
        \begin{algorithmic}[1]
        \State $cur = root$
        \For{\textbf{each} character $c$ \textbf{in} $S$}
            \If{$cur.nxt[c] == \textbf{null}$}
                \State $cur.nxt[c] = \text{new Node}$
                \State $cur.nxt[c].par = cur$
                \State $cur.nxt[c].pC = c$
            \EndIf
            \State $cur = cur.nxt[c]$
        \EndFor
        \State \text{append } $idx$ \text{ to } $cur.pos$
        \end{algorithmic}

        \textsc{Get-Link}($u, root$)
        \begin{algorithmic}[1]
        \If{$u.link == \textbf{null}$}
            \If{$u == root$ \textbf{or} $u.par == root$}
                \State $u.link = root$
            \Else
                \State $u.link = \textsc{Go}(\textsc{Get-Link}(u.par), u.pC)$
            \EndIf
        \EndIf
        \State \Return $u.link$
        \end{algorithmic}

        \textsc{Go}($u, c, root$)
        \begin{algorithmic}[1]
        \If{$u.go[c] == \textbf{null}$}
            \If{$u.nxt[c] \neq \textbf{null}$}
                \State $u.go[c] = u.nxt[c]$
            \Else
                \State $u.go[c] = (u == root) \text{ ? } u : \textsc{Go}(\textsc{Get-Link}(u), c)$
            \EndIf
        \EndIf
        \State \Return $u.go[c]$
        \end{algorithmic}

        \textsc{Search-String}($S, IDX, R, P, ans, root$)
        \begin{algorithmic}[1]
        \State $cur = root$
        \For{$i = 0$ \textbf{to} $S.length - 1$}
            \State $cur = \textsc{Go}(cur, S[i])$
            \State $tmp = cur$
            \While{$tmp \neq root$}
                \For{\textbf{each} $idx$ \textbf{in} $tmp.pos$}
                    \State $m = P[idx].length$
                    \If{$IDX < R$} 
                        \Comment{Horizontal match (Row extraction)}
                        \State $start = (IDX, i - m + 1)$
                        \State $end = (IDX, i)$
                    \Else 
                        \Comment{Vertical match (Column extraction)}
                        \State $start = (i - m + 1, IDX - R)$
                        \State $end = (i, IDX - R)$
                    \EndIf
                    \State \text{append } $(start, end)$ \text{ to } $ans[idx]$
                \EndFor
                \State $tmp = \textsc{Get-Link}(tmp)$
            \EndWhile
        \EndFor
        \end{algorithmic}

        \textsc{Aho-Corasick}($T, P, K, R, C$)
        \begin{algorithmic}[1]
        \State $List\_string = \textsc{Convert-Grid-To-Strings}(T)$
        \State $ans = \text{array of } K \text{ empty lists}$
        \State $root = \text{new Node}$
        \State $maxLen = \max(R, C)$
        \Statex
        \Comment{Build the Trie}
        \For{$i = 0$ \textbf{to} $K - 1$}
            \If{$P[i].length \leq maxLen$}
                \State $\textsc{Add-String}(P[i], i, root)$
            \EndIf
        \EndFor
        \Statex
        \Comment{Process all rows and columns}
        \For{$i = 0$ \textbf{to} $List\_string.length - 1$}
            \State $\textsc{Search-String}(List\_string[i], i, R, P, ans, root)$
        \EndFor
        \Statex
        \State $\textsc{Remove-Trie}(root)$ \Comment{Free allocated memory}
        \State \Return $ans$
        \end{algorithmic}

    \subsubsection{Step-by-step trace}
    \indent \textbf{Notation:}
        \begin{itemize}
            \item \textbf{Text $T$ (length $n$)}: The main text being searched.
            \item \textbf{Pattern $P$}: The string that we are looking for.
            \item \textbf{Shift ($s$)}: The current offset of the text T.
            \item \textbf{Positions ([(start position, end position), \ldots])}: A list storing the indices where a complete match is found.
        \end{itemize}

    \indent \textbf{Example:} text $T = [i, m, i, s, s, u]$ and list of pattern $P = [[u], [i, s, s, u], [s, s], [m, i, s, s]]$
    \begin{itemize}
        \begin{figure}[H]
            \centering
            \includegraphics[page = 1, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 1}
        \end{figure}
        \item We build a Trie based on the list of patterns. Grey nodes are output nodes, which means that the node represents an existing pattern from the list.
        
        \begin{figure}[H]
            \centering
            \includegraphics[page = 2, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 2}
        \end{figure}
        \item In this operation, we process the text $T$ character by character.
        If the current character matches a path from the previous node, we transition to a child node, which effectively updates the substring we have matched so far. 
        However, if no such path exists, we must follow a failure link to find the longest proper suffix and continue the search. 
        For example, when $T[0]$ is 'i', we move from the root to node 'i', updating our current state to represent the substring "i". \\
        Note: The yellow node indicates the current node (substring) reached.

        \begin{figure}[H]
            \centering
            \includegraphics[page = 3, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 3}
        \end{figure}
        \item In the second operation, we identify all suffix substrings of the current substring (currently "i") that exist in the Trie. 
        Since "i" has no proper suffix other than the empty string, we create a link pointing to the root, which represents the null state. \\
        Note: The green links are the longest proper suffix of each node.
        
        \begin{figure}[H]
            \centering
            \includegraphics[page = 4, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 4}
        \end{figure}
        \item Repeating the previous steps, we first attempt to move to the next node to match the substring 'im'. 
        However, because the Trie lacks an edge from 'i' for the character 'm', we must identify the longest suffix of 'im' that exists in the tree. 
        We follow the green links to check if any reachable node has a valid transition for 'm'. 
        We begin this process by following the green link of the parent node, as its longest suffix might lead to a valid path. 
        If no match is found, we continue traversing the green links until we find a valid node or reach the root. 
        In this case, since the root has an edge for 'm', we transition to node 'm', and our current matched substring becomes 'm'.
        \begin{figure}[H]
            \centering
            \includegraphics[page = 5, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 5}
        \end{figure}
        \item Following the second operation of the previous step, we look for all suffixes of 'm'. 
        Just as in step 1, the only proper suffix is the empty string; therefore, the new green link for node 'm' points to the root.

        \begin{figure}[H]
            \centering
            \includegraphics[page = 6, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 6}
        \end{figure}
        \item The next character is $T[2] = i$. Since node 'm' has a path for character 'i', the current substring becomes "mi".

        \begin{figure}[H]
            \centering
            \includegraphics[page = 7, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 7}
        \end{figure}
        \item To find the longest suffix of "mi", we check its parent node 'm'. 
        Since the green link of 'm' points to the root and the root has a path for character 'i', we create a new green link from "mi" to node 'i".
        Starting from node 'i', we follow the green links until we eventually reach the root node.

        \begin{figure}[H]
            \centering
            \includegraphics[page = 12, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 8}
        \end{figure}
        \item We will skip the steps for the next character, $T[3] = s$, to get the tree shown above.
        Following the next character, $T[4] = s$, we get the first pattern that matches to the substring of text is "miss".
         
        \begin{figure}[H]
            \centering
            \includegraphics[page = 13, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 9}
        \end{figure}
        \item Using the green link of the parent of "miss", we go to node "is". 
        Since node "is" has a path for character 's' pointing to node "iss", we create a new green link from "miss" to "iss".

        \begin{figure}[H]
            \centering
            \includegraphics[page = 15, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 10}
        \end{figure}
        \item Traversing the green links starting from node "iss", we find all suffixes of "miss": "iss", "ss", "s", and the root.
        
        \begin{figure}[H]
            \centering
            \includegraphics[page = 16, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 11}
        \end{figure}
        \item At node "miss", there is no path for the next character, $T[5] = u$. 
        Therefore, we follow its green link to node "iss", which has a path for 'u'. 
        This leads us to node "issu". 
        Since "issu" is an output node, we have found another pattern.

        \begin{figure}[H]
            \centering
            \includegraphics[page = 17, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 12}
        \end{figure}
        \item Following the previous step, we search for all suffixes. 
        First, we go to the parent node ("iss") and follow its green link to node "ss". 
        However, node "ss" does not have an edge for character 'u', so we follow its green link again to node 's'. 
        Since that node also lacks a path for 'u', we continue until we reach the root. 
        At the root, we find a path for 'u' leading to node 'u'. 
        Because node 'u' is also an output node, we have found another pattern. 
        Finally, node 'u' creates a green link pointing to the root, and the algorithm ends.

        \begin{figure}[H]
            \centering
            \includegraphics[page = 18, width=0.8\textwidth, trim={0cm 0cm 0cm 0cm}, clip]{data/Illustrations/ahocorasick.pdf}
            \caption[Figure \thefigure]{Step 13}
        \end{figure}
        \item After completing the Aho-Corasick algorithm, we have established all the green links for each node in the Trie. 
        We also identified four patterns that match substrings of the given text: "issu", "miss", "ss", and "u".
    \end{itemize}
    
    \subsubsection{Complexity}
    \begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{|l|c|p{8cm}|}
    \hline
    \textbf{Metric} & \textbf{Complexity} & \textbf{Reasoning} \\ \hline
    \textbf{Time Building Trie} & $O(\sum |P| \cdot 26)$ & This process happens before the search.
    We traverse every character of each pattern to build the Trie.
    For every node created, we must initialize an array of 26 pointers (equivalent to the alphabet size) to \texttt{nullptr}. \\ \hline    
    \textbf{Time Searching (Worst)} & $O(m \cdot R \cdot C)$ & Without exit links, we must traverse suffix links back to the root at every step.
    In the worst case (e.g., patterns are suffixes of each other), we might check $m$ nodes ($m$ is the length of the longest pattern) for every character in the grid. \\ \hline
    \textbf{Time Searching (Average)} & $O(R \cdot C)$ & In a random text, most steps do not find a match. This means we rarely need to climb the suffix links, and the search stays near the root of the Trie. \\ \hline
    \textbf{Time Searching (Best)} & $O(R \cdot C)$ & Occurs when characters in the grid never match any first character in the Trie, so the algorithm only does one comparison per index. \\ \hline
    \textbf{Auxiliary Space} & $O(\sum |P| \cdot 26)$ & The algorithm requires memory to store the Trie. The total number of nodes is determined by the total length of all patterns. 
    Each node contains an array of 26 pointers (equivalent to the alphabet size) that are initialized to \texttt{nullptr} upon creation. \\ \hline    
    \end{tabular}
    \caption{Complexity Analysis of Aho-Corasick Algorithm}
    \end{table}

    \subsubsection{Discussion}
    \begin{itemize}
        \item In Scenario 1, the number of patterns $K$ is small and the text size ranges from $10 \times 10$ to $500 \times 500$. 
        The Aho-Corasick algorithm is not time-efficient in this case, and it takes second place among algorithms that consume the most time to run.
        \item In Scenario 2, the number of patterns $K$ is large and the text is fixed at $100 \times 100$. 
        This scenario is the opposite of the last case; when $K$ becomes larger, Aho-Corasick's running time remains stable, which makes it the fastest algorithm as $K$ increases.
        \item In conclusion, the Aho-Corasick algorithm is more suitable for scenarios where $K$ increases over time. 
        When there are many patterns to query, it is more efficient because it is usually used for cases with many string matching queries on a set of patterns.
        \item Besides that, in both scenarios, it does not rely on character comparisons, so it is efficient for string-matching problems with a high comparison cost.
        So the Aho-Corasick algorithm is one of the best solutions for this problem.
        \item Optimization: We can add exit links that point directly to the nearest output node. 
        This means we do not need to traverse every node while following the green links to find a match. 
        By using this optimization, we improve the efficiency of the search phase, especially in the worst-case scenarios.
    \end{itemize}
        